# Apple Silicon platform

Apple Silicon is ARM64, not a separate architecture. Apple-specific boot,
UART, interrupt controller, device discovery, framebuffer, and power support
belong in this directory.
